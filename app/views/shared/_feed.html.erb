<% @feed_items.each do |feed_item|%>
<% if feed_item != nil %>
  <ol class="microposts">
    <%= render feed_item %>
  </ol>
<% else %>
 <ol class="microposts">
   feed_item=feed is nothing
<% end %>
<% end%>
<%= paginate @feed_items %>